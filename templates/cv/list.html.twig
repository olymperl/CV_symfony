{% extends 'base.html.twig' %}

{% block title %}CV Olympe Royère-Lafosse{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Mes {{type}}</h1>

    <ul class="list-group">
        {% for element in list %}
            <li class="list-group-item"><a href="/show_details/{{type}}/{{element.id}}">{{ element.titre }}</a></li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
